"""E&S Package Assembly — the second E&S workflow (Phase 3). Consumes Market
Matching's output directly (no re-matching); assembles a carrier-specific
submission package for broker review. Owned entirely by the E&S developer —
see docs/WORKFLOW_TEMPLATE.md for the package shape.
"""
